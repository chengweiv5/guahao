# 挂号 App v0.1：需求与实现方向讨论稿

状态：用户已确认在其它对话完成服务号普通付款和医保付款全流程，同意进入开发计划；本版主通路为微信服务号，互联网医院保留为独立研究渠道。医保脱敏证据支持同单 `1 → 7 → 6`，医保支付查询返回“支付成功”，手机端流程由用户确认。以下接入与执行细节纳入开发计划评审，尚未开始 Android 实现。目标设备为华为 Mate 60 Pro，鸿蒙 4.2.0。首个医院为北京佑安医院。

## 已确认的需求

医院通常提前 X 天，在指定时间开放目标日期的号源。用户提前设置就诊人、科室、医生、目标就诊日期及放号时间，由 App 到点自动尝试预约，避免必须守着手机或忘记放号。App 切到后台或手机锁屏后也要执行；形成待支付订单后停止抢号并通知用户付款。

自动尝试围绕一次放号事件执行。最长运行时长可配置，默认 30 分钟，从放号时间开始计算；提前取得有效订单就提前结束。提前检查登录和设备条件的时间不计入该时长。X 天和放号时间是可填写、可核对的医院规则，不能假定所有科室始终相同。

用户原话：“提前设置好就诊人，要挂号的科室和医生，以及告诉你放号的时间，到时间你自动挂号，然后通知我去付款。”

用户随后要求先完成 UI 设计；[18 页 Pencil 设计及交互约定](ui/2026-09-25-v0.1-ui-design.md)已经形成，使用眼科示例并统一“挂号任务”等产品用语。支付偏好优先医保；医保不可用时提示，不自动改为全额支付。服务号接口锁号后，按预设支付偏好准备订单；用户在手机微信“挂号结果查询”找到待支付订单并付款。普通付款与医保付款均已有单笔闭环证据；没有启用定时自动任务。

## 推荐的执行方式

| 方案 | 对本需求的适用性 | 取舍 |
| --- | --- | --- |
| 手机本地定时唤醒＋有通知的前台服务 | 推荐。等待期间保存任务，放号窗口内持续工作，满足锁屏与后台执行目标 | 不需要托管用户会话；需验证鸿蒙定时唤醒、后台管理和锁屏网络 |
| WorkManager 周期任务 | 可用于非精确的检查或恢复辅助，不用于放号执行主循环 | 官方周期任务最小间隔 15 分钟，执行时间也不是精确保证 |
| 云端定时执行 | 手机关机或离线时仍可执行，但超出当前需求 | 需要托管凭据、部署服务并维护通知链路，v0.1 暂不引入 |

建议以原生 Kotlin Android App 实现，手机接入微信服务号挂号接口；不使用 Google 服务作为必需依赖。具体 SDK、前台服务类型和电源权限按目标机实际兼容能力选择，不能从“鸿蒙 4.2.0”直接推断 Android API level。

## 服务号主通路的接入约定

首版面向用户本人使用，沿用已经验证的已有会话接入方式：用户在微信服务号正常登录并选择就诊人，主动把官方页面链接导入 App。App 只接受 `https://psc.hkinfo.net` 的已知页面路径，通过同源请求获得自身 Cookie，并向医院校验会话和当前就诊人；不读取微信存储，不把互联网医院票据当作服务号凭据。不能仅凭链接格式正确显示“登录成功”。若独立请求还缺少无法通过正常入口获得的 Cookie 或凭据，显示“需要重新接入”，不得启用真实任务。

首版自动任务只使用导入链接对应、且已取得完整凭据的当前就诊人。用户要换人时，先在微信切换，再重新导入并核对姓名等信息；跨人 `changePatient` 尚未验证，不自动遍历或切换。`id=oldId` 的同人会话请求也必须验证响应仍属于选定对象。重新导入不能覆盖正在提交或等待核对的任务身份。

认证链接、患者凭据和 Cookie 用 Android Keystore 管理的密钥加密后存于 App 私有目录；导入文本用完清空，不进入日志、通知、崩溃报告或系统备份。锁屏任务在用户首次解锁后可使用会话，不以每次生物识别为前置条件。重启后首次解锁之前不执行需要这些凭据的任务。

该导入方案是开发计划中的首版接入设计，Android 实测尚未完成；不宣称已实现微信初次 OAuth 或会话自动续期。放号前、执行中发现会话失效都停止新提交并提醒用户正常重新接入。

## 一个预约任务包含什么

| 内容 | 行为 |
| --- | --- |
| 医院、渠道 | v0.1 固定佑安微信服务号；与互联网医院会话和订单分开 |
| 就诊人 | 选择已绑定的就诊人；任务执行前核对身份仍有效 |
| 科室、医生 | 精确匹配所选项；不自动替换其他医生或科室 |
| 目标就诊日期 | 与放号日期分别保存；按“提前 X 天”推算时展示最终日期供核对 |
| 就医方式 | 展示服务号的“是否专程来京就医”等条件，由用户启用任务时核对；不把测试默认值用于所有任务 |
| 可接受时段 | 可选择上午、下午或具体时间范围；不填时展示“当天任意可预约时段” |
| 费用上限 | 启用前明确可接受上限；费用超出时不提交 |
| 支付偏好 | 优先医保移动支付；医保不可用时通知处理，不自行改为全额支付 |
| 放号时间 | 记录北京时间的明确日期和时刻，并展示倒计时 |
| 最长运行时长 | 用户可配置正整数分钟，默认 30；截止时间为放号时间加该时长 |

建议 v0.1 同一时间只执行一个任务，每个任务只取得一个符合条件的预约。多医生候选排序、并行多任务和自动付款不纳入本版方案；这是待评审的范围建议。

## 执行与停止规则

1. 保存任务时检查正常登录、就诊人、条件完整性、通知与定时运行权限。通过后显示“已就绪”，明确计划开始与截止时间。
2. 放号前执行准备检查；若登录已失效，提前通知用户重新登录。准备检查也受系统调度限制，不能保证在每次唤醒前均成功执行。
3. 定时唤醒后启动前台服务，通知栏持续展示任务阶段与停止入口。放号前只准备，放号时发起首轮查询。
4. 用服务端返回的医生、日期、时段、余号和费用匹配条件。初步建议查询间隔不短于 5 秒、每次只有一个请求在途；具体节奏受医院限制和真机测试约束。限流时退避，不并发刷接口。
5. 匹配后核对任务条件及服务号功能校验，调用一次 `lockRegis`。不跨医生、日期或费用自动降级，收到接受处理不等于锁号完成。
6. 通过 `queryRegisStat` 核对异步结果，取得订单号后用 `getRegisList` 精确匹配。状态为 1 且需要付款时停止查号；预设优先医保且条件允许时调用一次 `sxPayCN` 初始化，并回查状态 7 后通知到手机结果页“医保缴费”。初始化结果不明时回查，不再次锁号、不透明重试初始化。状态 2 为普通预约完成分支，状态 6 为医保已支付分支；医保付款还用 `queryRegisYbPayState` 同单核对。零元、候补或未知状态分别处理。
7. 达到配置的截止时间后，不发起新的查询或提交。在截止前已发出的提交若结果未明，仍需完成有限的结果核对并提示用户，不能假装它没有发生。
8. 用户停止后立即阻止新请求；已经发出的锁号不能撤回，仍核对它是否形成订单。停止任务不等于自动取消医院预约。

计划中的默认执行参数：查号至少间隔 5 秒；网络失败按 5、10、20、40、60 秒退避并优先遵守服务端等待时间；一次只允许一个请求在途。锁号接受后按官方异步语义查询结果，最多 30 次、间隔至少 1 秒，随后转为订单核对。一次提交从发出起最多自动核对 120 秒，超过后标记“结果待人工核对”，不宣称失败，也不再次锁号。上述参数是首版保守实现值，不是医院频率承诺。

最长运行时长按正整数分钟保存，默认 30；例如设为 60 分钟不会仍在第 30 分钟结束。延迟唤醒、断网和进程重启不重置截止时间。用户修改运行中的时长需先停止任务并处理未决提交，再新建任务。系统允许的前台运行时长不足时，启用前明确提示，不能静默截短。持续运行使用单调时钟辅助截止判断，系统时钟大幅改变时重新核对并提示。

## 提交异常与恢复

查询失败可以有限退避重试。锁号请求超时、连接断开或进程退出时，必须进入“结果待核对”，不能直接重新锁号。

有订单号时从服务号预约列表精确匹配，本次已验证成功。无订单号时按患者、日期、医生、时段和提交时间关联的恢复策略尚未实测；列表暂时为空不代表请求一定失败。无法唯一确认时暂停任务并通知用户核对，不继续自动提交。

提交前记录选定条件、当前订单列表基线及唯一尝试编号，持久化成功后才允许发出锁号请求。网络层关闭锁号请求的透明重试；多个唤醒入口通过同一持久化执行权串行处理。`queryRegisStat` 只按用户查询，返回值不能单独证明属于本次任务，必须再用订单号及就诊条件核对。提交期间即使用户在官方页面操作，也不得把其它订单错误认领为本次成功。

任务状态、条件和提交尝试记录持久化；运行时长和订单付款剩余时间分别计算。重启恢复时先检查截止时间与是否有未确认的提交。登录缺失、权限被撤销或结果未明时恢复为需要处理的状态，不悄悄提交。

## 通知与付款

使用手机本地通知：任务执行中提供停止入口；待付款、未挂上和需处理异常提供明确结果。锁屏默认隐藏就诊人姓名等个人细节，点入 App 后查看医生、日期、金额和订单。

待付款通知以服务号订单状态和 `invalidtime` 为准；通知应显示实际可确认的截止时间，不能沿用任务运行时长。支付由用户操作。

已验证付款交接为“接口锁号 → 选择医保并初始化 → 通知 → 手机服务号挂号结果查询 → 医保缴费 → 用户授权付款 → 同单回查”。官方结果页按 `source=2` 与状态路由：状态 1 的 `sendPay()` 是普通缴费；状态 7 的 `sendYbPay()` 是医保缴费，会在手机重新请求本单医保授权参数。此前仅看普通缴费函数而认为结果页不能接续医保的结论已纠正。

`sxPayCN` 的 `data` 是包含 `pageAuthCode,sceneId,transId` 的 JSON 字符串，不是网页链接；App 初始化后无需把这些临时参数传给手机或持久保存。手机通过官方入口自行重新取参数并唤起医保小程序。独立 `POST /order/queryRegisYbPayState` 返回 `code=0`、“支付成功”与同单状态 6 一起构成医保付款完成证据；`code=2` 为待付或支付中。状态 7 时 `isyb` 可能仍为 0，不能据此回退全额支付。

普通与医保样例的付款窗口均约 30 分钟，医保初始化不延长期限；产品逐单读取 `invalidtime`，与用户配置的任务运行时长分别计算。此前互联网医院订单未出现在服务号结果列表，不依赖跨渠道同步。第三方 APK 直达具体微信页面和回跳仍未验证，首版提供准确的官方人工入口，不将它误列为医保接口闭环未知项。


## 数据模块与本机保存

- 任务执行模块：管理定时唤醒、条件匹配、截止时间、单次提交和异常恢复；页面只发出创建、启用、停止等用户意图。
- 医院接入模块：集中处理医院请求头、会话、响应解析、号源与订单语义。读取响应保留原始状态码，未知状态不当成成功或无号。
- 本地状态模块：保存任务、必要的提交恢复记录及通知结果。会话凭据使用 Android Keystore 保护；认证方式按服务号初次授权实测再定。日志不包含票据和完整患者资料。
- 付款交接模块：处理已验证支付入口及返回后的订单回查，不把“打开了支付页面”当成支付成功。

先为一个医院建立清晰模块，不预建通用多医院平台。互联网医院研究证据继续保留，待后续确需第二个渠道时再抽象差异。

## 真机验收与目前证据

已验证：服务号已有会话、就诊人列表、科室排班、分时余号、单笔锁号、手机“挂号结果查询”订单可见、普通订单状态 2、医保初始化与状态 7，以及医保支付查询成功和同单状态 6。用户已确认手机医保流程完成。互联网医院独立短信登录与查询另有研究证据。未验证：服务号全新认证、会话自然失效恢复、异常恢复、Android 集成后的入口/回跳和 Mate 60 Pro 真机后台行为；医保金额字段含义及明细票据另行核对，不影响已验证的医保付款成功结论。

开发验收需覆盖：提前设置后长时间锁屏到点唤醒、切后台、Wi-Fi 与移动网络切换、短时断网、通知与电源权限变化、进程被系统回收、重启后任务恢复、时钟修改，以及提交时断网后不重复预约。任务不能在用户强行停止 App、关机或无网络时承诺继续执行。

先以模拟医院响应验证提交状态机、截止规则和恢复行为，再在目标机验证真实认证与查询。复用已有服务号全流程证据，不为重复证明接口可用而新增预约；Android 集成后的真实提交验收仅在用户安排下一笔真实就医需求时执行。医院接口全流程验证与 Android 定时执行验收分别记录。

Android 官方说明：

- [前台服务](https://developer.android.com/develop/background-work/services/fgs)：运行时需可见通知，但并不自动豁免所有省电限制。
- [Doze 与 App Standby](https://developer.android.com/training/monitoring-device-state/doze-standby)：锁屏深度空闲可能限制网络和唤醒锁，豁免也仅是部分豁免。
- [定时闹钟](https://developer.android.com/develop/background-work/services/alarms/schedule)：精确定时受系统版本和用户权限约束，重启或权限变化需重新检查。
- [后台启动前台服务限制](https://developer.android.com/develop/background-work/services/fgs/restrictions-bg-start)：存在用户请求的精确闹钟等例外，具体适用性需结合目标系统。
- [WorkManager 周期任务](https://developer.android.com/develop/background-work/background-tasks/persistent/getting-started/define-work)：周期最短 15 分钟，不作准点抢号执行器。

接口证据见 [研究记录](../research/2026-09-24-youan-api-feasibility.md)、[服务号单笔闭环](../research/2026-09-24-youan-service-account-booking-result.md)和[医保分支核对](../research/2026-09-24-youan-medical-insurance-payment.md)。已交付的[飞书调研文档](https://bytedance.larkoffice.com/docx/FNWddvIaOot8CRxS5IycdO3mned)保留历史来源链；当前实现范围以本地最新设计和[开发计划](../superpowers/plans/2026-09-24-service-account-v01.md)为准。开发计划评审不等于授权新的真实就医预约。
