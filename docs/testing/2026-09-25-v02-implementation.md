# v0.2 第一批实现与接入阻塞记录

开发始于 2026-09-25，验证跨至 2026-09-26。状态：**部分实现已落地，完整北京平台接入尚未完成。** 用户已明确 v0.1 完成，原等待上游的条件已解除。本记录不能作为 v0.3 启动依据。

## 2026-09-26 真机接入增量

独立临时包内官方京通 WebView 已完成用户手机号登录；重启后服务端 `auth/user/get` 为 `200 / 0000` 且身份存在。项目实际 Kotlin 客户端通过两院目录、科室、日历、医生与时段解析。新增 9 项查询/解析测试后 app debug/release 各 40 项通过，双构建和 lint 通过、仍各 13 条原有 UseKtx 警告。随后新增患者解析与467拒绝处理测试，最终计数见机器证据。

经用户扩大只读授权，患者列表返回 1 位患者、2 张卡；最近 30 天订单接口成功但为空，不能证明非空订单解析或同单核对。114 全新独立会话返回 3087、要求微信授权，不能套用京通结论。正式 App 已接入隔离连接 Activity 和公开查询服务，患者选择、提交及订单核对仍未完成；本次没有真实预约。详见[独立验证记录](../research/2026-09-26-independent-webview-verification.md)与[脱敏证据](evidence/v0.2/independent-webview-query.json)。

下方首批实现记录保留发生时状态，原生 HTTP 的 202/HTML 结果仍成立，不能延伸为独立 WebView 不可用。

## 实际实现

- 四步设置：医院／渠道、就诊条件、执行设置、确认启用。固定页头和底部主操作，中间独立滚动；返回仍有效步骤保留选择，更换医院／渠道／患者／科室／日期后重新查询。
- 青绿色 UI、精简任务卡、医院筛选、任务数量摘要；卡片保留医院、院区、渠道、医生、日期与任务状态；已完成订单不再显示付款操作。
- 首次连接在编辑器内打开独立界面，返回仍在原步骤。就诊日期、医院放号时间与任务运行窗口维持 v0.1 校验。
- 北京 114 与京通独立渠道模型，院区进入连接身份比较；旧序列化数据缺省院区可继续读取，无数据库升级或凭据改写。
- 北京平台只读客户端：医院目录、二级科室、日历；按官方公开脚本设置 `Request-Source`，严格校验 HTTP、媒体类型、业务码和结构。未知日历文案保留未知状态；错误信息不含服务端原文、Cookie 或身份内容。
- 北京平台客户端无自动重试、不跟随跳转、不持有 Cookie；HTML 校验页、限流、业务错误和明确未登录分开处理。目录界面支持重试、已加载医院搜索、分页与渠道切换时取消旧请求。

**尚未实现／验证：** 正式 App 独立连接流程、患者列表接入、实际提交、同单核对和支付状态。独立临时包内认证、患者只读和医生查询已于上述增量中验证。北京 114／京通自动任务入口因此仍不可启用，未提供虚构的认证成功或提交结果。v0.1 佑安真实入口继续保留。

## 原生请求实测

一次宿主网络只读 `POST https://www.114yygh.com/jtjk/mobile-service/hospital/list`，使用公开业务脚本中的 `Request-Source: WE_CHAT` 和已观察的目录字段；没有用户凭据，没有代理或 CA。

结果为 **HTTP 202、`text/html; charset=utf-8`**，不是医院 JSON，不能认定目录接入成功。仅保存[脱敏结果及回包摘要](evidence/v0.2/native-query-probe.json)，未入库原始 HTML 或动态校验参数。未分析或仿造校验脚本，未登录、读取患者／订单、预约或付款。

官方业务脚本还确认 `WE_CHAT` 与 `JT_WECHAT` 是不同渠道值。普通查询适配层可以共享，但当前没有证据允许两个渠道互用会话。此前手机官方页面的两医院查询成功仍是页面观察证据，不是这次原生请求成功。

当前 `adb devices` 只有本轮创建的模拟器，没有 USB 手机。已向用户请求接回手机并停在已登录京通首页；后续继续正常认证及查询验证。不能以时间流逝推定用户已连接或授予真实预约权限。

## 验证与环境

基线：v0.1.0 `3d58fb0d0a108fd5169a3fd9add34ded27d28ce5`；已批准设计 `081d22c`。本地工作分支 `codex/v02-beijing-platform-design`。

使用 JDK 21、Android SDK 36；字节码目标 Java 17。API 31 模拟器通过 `-read-only -no-snapshot-save` 临时副本运行，没有改动原 AVD 数据或手机应用。所有测试内容为虚构样本。

```sh
./gradlew :core:test :app:testDebugUnitTest :app:testReleaseUnitTest \
  :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease \
  :app:lintDebug :app:lintRelease
```

- 单元测试：core 51／51，app debug 31／31、release 31／31。
- debug／release／AndroidTest 构建通过；lint 两构建均 0 错误，各 13 条原有 `UseKtx` 警告。
- UI、连接、存储、并行及构建模式的最终设备验证见[机器可读汇总](evidence/v0.2/implementation-verification.json)。
- 小屏 360dp，以及 412dp／1.3 倍字体检查底部操作可见；截图经本机目视与文字检查，没有把设计图替代运行截图。
- release 只在临时模拟器用调试签名副本运行模式测试，未签署发布包、未安装真机或分发。

## 中间失败与修复

1. 一次安装早于 AndroidTest 构建结束，新增测试类未进入当时安装包；等待构建完成后重新安装运行。
2. 旧医保流程测试复用已有演示订单日期，被正常防重复保护拦住；改用本机未使用的测试日期。未降低真实重复订单保护。
3. 并行服务通知测试固定等 1 秒，偶发早于通知更新；改成最多 3 秒条件等待，并在放号前确认两个任务，再验证停止一个不影响另一个。
4. 一次 debug 回归尚未结束就安装了 release，测试进程被安装打断；随后按完成回读后再换包的顺序重新运行。
5. release 隔离测试期望中缺少 v0.1 数据库存储时自动补全的连接 binding；补全合成 fixture，继续完整比较记录，不忽略任务／订单字段。

## 保存与恢复

本批代码仅本地保存，不标记完整 v0.2，不推送、不通知 v0.3 启动。用户此前对“开发验证完成后推送”的授权继续保留。

修改前 Git 备份：`codex/backup-v02-before-implementation-20260925`；文件副本：`/tmp/guahao-v02-implementation-20260925/`。可以从备份分支创建单独工作区检查旧版本，无需强制重置当前分支。新数据仍是 v2 数据库，不清空或覆盖业务库，也不提供降级到 v1 数据库的回滚方式。
